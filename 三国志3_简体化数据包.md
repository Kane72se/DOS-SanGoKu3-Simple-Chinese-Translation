# 三国志3（DOS 繁体版）简体化 — 数据包

> 用途：在新 Codex 对话中继续维护《三国志3》简体化成果。本文档汇总字库格式、逆向结论、版本历史、文件位置、验证方法与已知限制，全部信息可独立使用，无需回看旧对话。

日期：2026-08-15 · 当前版本：**v22（最终）**

---

## 1. 项目是什么

把 DOS 版《三国志3》（光荣 KOEI，繁体中文）的显示文字简体化。采用**字模替换**方案：

- 不改文本编码、不改程序代码，只重建字库 `HAN.16P` 中的点阵字形。
- 游戏仍按原编码读取文本，但显示出来的是简体字形。
- 旧存档可正常读取（文本字节未动）。

---

## 2. 字库格式（核心结论）

- `HAN.16P`：**37380 字节 = 1335 槽位 × 28 字节/字**。
- 每槽 = **14 行 × 16 像素**（每行 2 字节，高位在前），无表头、行优先。
- 文本编码：游戏正文为 **BIG5**；新武将名字（NBDATA.DAT）用 `D9 xx` 双字节游戏内码。
- 重大发现：游戏运行时使用**自定义字符集**（非标准 BIG5），游戏码（如 開=A1E3）与标准 BIG5（開=B67D）吻合率为 0%；按游戏码排序呈“字典/部首-笔画”顺序。**映射表只存在于运行时**（DS:1A9C，1333 项 × 2 字节），这是早期静态提取失败的根因。

### 2.1 简体字形生成方法（v18 起）

- 参考源：GB2312 `HZK16` 16×16 点阵优先；缺字回退 SimSun 16px 渲染。
- **16 行 OR 压缩成 14 行**（不再是 v14-v17 的裁切），避免简体字上下缺行：
  ```
  输出行 r ← 输入行 r*16//14 .. (r+1)*16//14 的按位 OR
  ```
- 生成脚本参考：`work\build_v18style.py`、`work\build_v21.py`、`work\build_v22.py`。

---

## 3. 当前状态（v22，最终版）

- 槽位映射表：**1335 槽全部标注，未知槽位 0**。
- 繁简不同、已改写简体字形：**448 槽**。
- 繁简同形槽位（把/力/兵/用/食/空/四 等）：字形本就正确，仅补全映射。
- 字节数 37380 不变；与 v21 相比仅 7 个槽位字模不同。
- 已放入游戏目录 `Downloads\dos\san3\HAN.16P`（原 v21 备份为 `HAN.16P.bak_v21`）。

**最终字库 SHA256：**
```
ACCF69883686F3617BF9B76C23C8EDE199F381740E991749ED67EA8B92BEFAEF
```

---

## 4. 逆向方法论（已跑通的路径）

### 4.1 槽位 → 汉字 映射的来源

1. **运行时字符表导出**（权威）：DOSBox-X 调试器 `MEMDUMPBIN` 导出 DS:1A9C 的 1333 项“槽位→游戏码”，文件 `运行时槽位字符码表.bin`（2666 字节），与指令流日志提取的 732 项完全吻合。
2. **武将名单交叉验证**：`SNDATA1B~6B.CIM` 每个武将记录含 2-4 个游戏码名字，解码后对照真实武将（袁紹/曹操/劉備/關羽/諸葛亮/孫權…）确定映射。
3. **屏幕像素 ↔ 字库字节零误差比对**：DOSBox 截图逐字提取字形，与槽位点阵求汉明距离/覆盖匹配，用已知字校准后定位未知槽。
4. **用户人工辨认**：v22 最后 37 个未知槽位由用户看图辨认补齐。

### 4.2 绘制链路（调试器确认）

```
游戏码 → 线性查表(094F:4D80) → 槽位 → 槽位×28 字形偏移 → 字形
```

绘制代码段 094F（MAIN.EXE 加载后），直接写 VGA 端口（03C4/03CE），不走 INT 10h。

---

## 5. 版本历史（v4 → v22）

| 版本 | 要点 |
|---|---|
| 第一阶段 | 锚点验证 15 菜单字 + 7 补完计划字，替换 10 字模，主菜单简体化 |
| v4-v14 | 字模替换早期版本，逐步解出锚点与常用字；v14 采用 16 行裁切 |
| v16 | 修复 11 处映射表错误（泉→呂、巫→兒、林→樊、鄒→許 等），新增 39 槽定位 |
| v17 | 追加 110 处错字修正，利用武将能力值交叉验证 |
| v18 | **改为 16 行 OR 压缩成 14 行**，解决简体字上下缺行问题 |
| v19 | 首次用“武将数据能力值↔武将表”交叉验证，解出 15 未知槽 + 修正 110 错字 |
| v20 | 修复 8 个界面字（现/处/去/吗/蓄/隆/死/龙回退），完成城市/战场名修正 |
| v21 | 修复 號(857)/還(267)/廬(1321)，补写着/须/壶/邺 |
| v22 | 用户辨认补齐最后 37 个未知槽位（諜/強/勢/舉/職/認/驃 等），**未知清零** |

各版本详细说明见 `outputs\san3_简体化_全量逆向\v16~v22更新说明.md`。

---

## 6. 已知限制（重要）

- **一字多用槽位**：原版字库部分槽位被多个近似字共用（如 張郃 原版显示“張和”、劉琮 显示“劉綜”），为忠实还原原版而保留，非 bug。
- **超纲简体字**：个别字（如 𬘭）SimSun/HZK16 无字形，保留原字形。
- **图片内嵌字**：状态栏“變更為”的“為”等是图片内嵌字，不属于 HAN 字库，字库替换无法修改。
- **名字数据**：NBDATA.DAT 与 NAME.16P 未动（按约定只改 HAN.16P）。

---

## 7. 文件位置

| 文件 | 路径 |
|---|---|
| 最终字库 | `outputs\san3_简体化_全量逆向\HAN.16P_全量v22版` |
| 最终映射表 | `outputs\san3_简体化_全量逆向\槽位映射表v22.tsv` |
| 工作映射表 | `work\han_map_v20.tsv`（同内容，修改器共用） |
| 历史字库 | `outputs\san3_简体化_全量逆向\HAN.16P_全量v4~v21版` |
| 历史映射表 | `outputs\san3_简体化_全量逆向\槽位映射表v4~v21.tsv` |
| 运行时字符码表 | `outputs\san3_简体化_全量逆向\运行时槽位字符码表.bin`（1333 项×2B） |
| 捕获对 | `outputs\san3_简体化_全量逆向\captured_pairs.tsv`（732 项） |
| 更新说明 | `outputs\san3_简体化_全量逆向\v16~v22更新说明.md` |
| 分析文档 | `全量解码进展v8.md`、`动态反编译可行性报告.md`、`数值与数据结构笔记.md`、`说明.md` |
| 生成脚本 | `work\build_v18style.py`、`build_v21.py`、`build_v22.py` |
| 字形渲染/匹配脚本 | `work\render_unknown.py`、`match_glyphs.py`、`verify_user_chars.py` 等 |
| 游戏目录（简体） | `C:\Users\admin\Downloads\dos\san3\` |
| 繁体原版（对照） | `C:\Users\admin\Desktop\san3dos\san3\` |

---

## 8. 常见操作速查

### 8.1 替换字库

把 `HAN.16P_全量v22版` 复制到游戏目录改名为 `HAN.16P`（先备份原文件），DOSBox/DOSBox-X 运行 `PLAY.BAT`。推荐 `opengl perfect` 输出、保持 4:3 比例。

### 8.2 槽位字形查看

`work\render_unknown.py` 可把任意槽位渲染成 PNG（16×14 放大 8 倍），用于人工辨认或与截图对比。

### 8.3 新发现错字时的处理流程

1. 从游戏截图提取字形像素（参考 `match_glyphs.py` / `final_verify.py`）。
2. 与槽位点阵比对定位（用同屏已知字校准方法可靠性）。
3. 修改 `work\han_map_v20.tsv` 对应槽位。
4. 跑 `build_v22.py` 逻辑增量重建（只改目标槽位，保持其它槽位不动）。
5. 验证字节数 37380、差异槽位列表、放入游戏目录测试。

---

## 9. 相关项目文件

- 修改器数据：见同目录 `三国志3_修改器数据包.md`。
- 画面上下黑边逆向：见同目录 `三国志3_画面黑边逆向数据.md`。
- GitHub 仓库：`Kane72se/DOS-SanGoKu3-Simple-Chinese-Translation`（简体化 + 修改器 + README 中英双语）。
