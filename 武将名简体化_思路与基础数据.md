# 三国志3 武将名字简体化 — 思路与基础数据

## 一、原理：为什么改字库就够了

三国志3 的正文（包括武将名）是**自定义编码**，程序把"字码 → 字库槽位 → 字模"渲染到屏幕。`HAN.16P` 是 1335 个 16×14 点阵字模组成的字库，**只要替换字模，不改任何文本和程序，显示就会变简体**。目前 v14 版已按你逐屏反馈修复了 400+ 槽位。

## 二、这份武将表怎么用

`三国志3武将列表_简体对照.xlsx` 由你桌面的原始表生成，每列：

| 列 | 说明 |
| --- | --- |
| 繁体名 | 游戏内显示的名字（已把表格里的简体误录还原成繁体，如"阿会喃"→"阿會喃"） |
| 简体名 | 用简繁转换得到的标准简体名 |
| 读音 / 知力 / 政治 / 武力 / 魅力 / 陸指 / 水指 | 原表数值，可用来在游戏里核对同一武将 |
| 逐字字库状态 | 名字每个字的槽位状态：`已简体`=v14 已替换、`同字形`=繁简同形无需改、`未识别槽位`=该字在映射表还没定位到槽位、`按简体映射`=按简体反查 |
| 待处理字 | 把"未识别槽位"的字汇总出来，一眼看出这个武将名还有哪些字没处理 |

## 三、待处理字的重点发现

203 个武将名含有"未识别槽位"的字。高频的字大多是**日文新字体**（游戏日版转繁体中文时残留），例如：

- 呂（12 人）、呉（8）、厳（6）、蔡（6）、関（5）、楽（4）、龐（4）
- 糜、攸、霊、栄、勲、昂、鮑、韋、植、蒯、徳、暹、顔、続、権、秀、瓊、児、諶、爽、繇、瑩、叡、観、闓、承、達、苞、豊、満、璜、樊、琦、粛、封、阿、黙、煕、圃、垢、粲、詡、霍、綝、図、淮、恩、恵、倹、莒、闞、沢、鞏、敘、蕤、喬、貢、環、翻、邢、綱、畯、輿、瓚、沛、覧、仲、崔、渙、蹟、師、旌、冑、沙、摩、魴、光、讃、褒、彧、惲、挙、毘、綜、奐、憲、丕、髦、熊、鵠、韶、翊、膺、顗、嶷、衢、広、紘、済、蟬、邈、翼、遼、応、鄭、荼、旻、竜、謖、鉄、疆、濬、褘、巽、彤、卞、喜、歩、騭、毛、玠、優、懐、密、齢、遜、永、焉、潰、晙、寛、虔、曠、冷

这些字大多**繁简转换后与原字不同**（如 呉→吴、厳→严、関→关、楽→乐、図→图、応→应、広→广、児→儿、沢→泽、続→续、権→权、韋→韦、達→达、遜→逊、遼→辽），说明在游戏里很可能仍显示繁体/日文字形，是后续要逐一处理的对象。

## 四、全量替换武将名字的做法

1. **以这份表为清单**：每行武将名的"待处理字"就是要修的字。
2. **逐字定位槽位**：对每个待处理字，进游戏找到该武将的信息界面截图（数值可以帮忙锁定武将），用像素匹配定位字库槽位，再改映射并重建 HAN.16P（就是你一直在用的反馈循环）。
3. **批量思路**：高频字（呂呉厳蔡関楽龐等）一次定位、多处生效——同一字在不同武将名里共用同一个槽位。
4. **验证**：替换后用数值（知力/武力/魅力）在游戏里核对武将，简体名和数值能对上即成功。

## 五、当前文件

- `三国志3武将列表_简体对照.xlsx`：532 个武将的繁简对照 + 字库状态 + 待处理字清单。
- `HAN.16P_全量v14版`：当前字库（407 槽简体化）。
- `槽位映射表v14.tsv`：槽位→汉字映射。
