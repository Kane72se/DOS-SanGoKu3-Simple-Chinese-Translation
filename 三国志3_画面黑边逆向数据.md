# 三国志3（DOS 繁体版）画面上下黑边 — 逆向数据包

> 用途：在新 Codex 对话中继续研究“去掉游戏画面上下黑边”。本文档汇总已确认的事实、证据文件位置、调试数据与下一步建议，全部信息可独立使用，无需回看旧对话。

日期：2026-08-15

---

## 1. 问题现象（已确认）

- 游戏在 DOSBox-X 中运行时，画面为 **640×480**，但**上下各有 40 像素纯黑边**，实际游戏内容区域为 **640×400**（y = 40..439）。
- 用户明确：黑边是**游戏画面自带的**，与 DOSBox 无关（换用不同渲染器/输出方式均存在）。
- 目标：找到黑边的产生机制（显示模式设置或绘制范围限制），实现“去黑边”（让 640×400 内容铺满或裁剪为 640×400 输出）。

### 1.1 截图像素分析（程序化验证）

对用户提供的游戏内截图（640×480）逐行分析：

| 截图 | 顶部黑边 | 内容行范围 | 底部黑边 | 内容高度 |
|---|---|---|---|---|
| `user_caps\main_000.png` | y=0..39（纯黑） | y=40..439 | y=440..479（纯黑） | 400 行 |
| `user_caps\main_004.png` | y=0..39（纯黑） | y=40..439 | y=440..479（纯黑） | 400 行 |

判定方法：逐行统计非黑像素，内容行从 40 开始、到 439 结束，精确 400 行。上下各 40 像素。

---

## 2. 显示模式证据

### 2.1 BIOS 数据区当前视频模式字节（关键）

在调试器转储 `kbbuf.txt` 的 BIOS 数据段中：

```
0040:0000003E  00 00 00 00 00 00 00 00 00 00 00 12 50 00 00 A0
```

偏移 **0040:0049 = 0x12**（十进制 18）= **VGA 640×480 16 色图形模式**（mode 12h）。

结论：**游戏确实运行在 640×480 模式下**，黑边不是“游戏运行在 640×400 模式”造成的，而是游戏自己只在中间 400 行绘制内容（或显示起始位置偏移）。

### 2.2 DOSBox-X 日志中的表面尺寸

`work\dbg.log` 中可见：

```
pixratio 1.000, dw false, dh false
Aspect ratio: 640 x 480  xToY=1.333 yToX=0.750
surface consider=640x480 final=640x480
```

DOSBox 表面为 640×480，与截图一致。

---

## 3. 关键逆向线索：游戏直接写 VGA 寄存器绘制

`LOGCPU.TXT`（16 MB CPU 指令日志，DOSBox-X 调试器输出）显示游戏运行时代码段 **094F**（应为 MAIN.EXE 解压/加载后的代码段，DS=54E4，ES=A000 时为显存操作）。

### 3.1 绘制例程寄存器设置序列（094F:24B0 附近）

```
094F:000024B2  mov  dx,03CE          ; 图形控制器端口
094F:000024B5  mov  ax,0001
094F:000024B8  out  dx,ax            ; 索引1 = 置位/复位
094F:000024B9  mov  al,03
094F:000024BB  out  dx,ax            ; 索引3 = 数据循环移位
094F:000024BC  mov  al,05
094F:000024BE  out  dx,ax            ; 索引5 = 模式寄存器
094F:000024BF  mov  ax,FF08
094F:000024C2  out  dx,ax            ; 索引8 = 位掩码 FF
```

### 3.2 清屏/填充序列（094F:2BA9 附近）

```
094F:00002BA9  mov  dx,03C4
094F:00002BAC  mov  ax,0F02
094F:00002BAF  out  dx,ax            ; 序列发生器索引2 = 全部 4 平面
094F:00002BB0  mov  dx,03CE
094F:00002BB3  mov  ax,0205
094F:00002BB6  out  dx,ax            ; 图形控制器索引5 = 写模式2
...
094F:00002BC0  mov  cx,dx
094F:00002BC2  repe stosb            ; ES:A000 显存逐行填充
094F:00002BC4  add  di,0050          ; 每行 +0x50 = 80 字节（640 像素 / 8 / 平面）
```

要点：
- 游戏直接写 VGA 端口（03C4 序列发生器、03CE 图形控制器），**不通过 INT 10h**（日志中未捕获到 `int 10`）。
- 每行 0x50=80 字节 → 640 像素宽、每字节 8 像素（4 平面模式），与 640×480 16 色一致。
- `repe stosb` 循环填充范围由调用方传入的行数决定——**若黑边是“只填/只画 400 行”，控制行数的参数就在调用方**。

### 3.3 未发现的内容

- `LOGCPU.TXT` 中**没有** 03D4/03D5（CRTC 端口）写入记录、也没有 `int 10` 记录 → 显示模式可能由 OPEN.EXE 设置（日志窗口从主菜单才开始记录），或模式初始化代码未被当前日志覆盖。
- 在 OPEN.EXE / MAIN.EXE 文件本体中直接搜索 `int 10` / `out 3D4` **无命中** → EXE 内容可能被压缩/加密，运行时才解出（与 3.1/3.2 的 094F 段为运行时代码一致）。

---

## 4. 黑边产生机制假设（按可能性排序）

1. **绘制范围限制**：游戏以 640×400 为逻辑画布，每帧只绘制/只清 y=0..399（对应显存行 40..439 是偏移后的结果），或绘制循环上限 = 400 行。→ 修改点：绘制/清屏循环的行数参数或画布高度常量。
2. **显示起始地址偏移**：通过 CRTC 起始地址寄存器（3D4 索引 0x0C/0x0D）或类似机制把画面垂直居中，导致上下各 40 行不显示。→ 修改点：模式初始化时 CRTC 寄存器序列。
3. **分辨率协商**：游戏请求 640×400 模式，但 DOSBox/BIOS 回落到 640×480（0x12），内容居中。→ 修改点：OPEN.EXE 的模式设置代码。

---

## 5. 相关文件位置

| 文件 | 路径 | 说明 |
|---|---|---|
| CPU 指令日志 | `C:\Users\admin\Documents\Codex\2026-08-10\zha\LOGCPU.TXT` | 16 MB，DOSBox-X 调试器逐指令日志，含 094F 段 VGA 操作 |
| 调试日志 | `C:\Users\admin\Documents\Codex\2026-08-10\zha\work\dbg.log` | DOSBox-X 启动日志，含 surface 尺寸、模式信息 |
| BIOS 数据区转储 | `C:\Users\admin\Documents\Codex\2026-08-10\zha\work\kbbuf.txt` | 含 0040:0049=0x12 模式字节 |
| 寄存器转储 | `C:\Users\admin\Documents\Codex\2026-08-10\zha\work\regs.txt`、`dt_regs.txt` | 调试器断点现场 |
| 用户截图 | `C:\Users\admin\Documents\Codex\2026-08-10\zha\work\user_caps\main_000.png`、`main_004.png` 等 | 640×480，内容 40..439 |
| 游戏目录（简体 v22） | `C:\Users\admin\Downloads\dos\san3\` | OPEN.EXE、MAIN.EXE、PLAY.BAT 等 |
| 繁体原版（对照） | `C:\Users\admin\Desktop\san3dos\san3\` | 原版 OPEN.EXE/MAIN.EXE |
| DOSBox-X 调试器 | `C:\DOSBox-X\` | 内置调试器（debuggerrun=debugger、logfile、bpint、bpm 等） |

### 5.1 分析脚本（旧对话遗留，可参考/复用）

- `work\logcpu_analyze.py`：统计 LOGCPU.TXT 中各代码段（CS）执行范围。
- `work\koei_disasm.py`：capstone 16 位反汇编示例（KOEI.COM）。
- `work\analyze_screen.py` / `screen_match.py`：屏幕像素分析。
- `work\test_disp1.conf` / `test_disp2.conf` / `test_disp3.conf`：直达主菜单的 DOSBox-X 测试配置（`fmdrv.com` + `main.exe`）。
- `work\run_*_flow.ps1`：自动化游戏流程脚本（含调试器交互），可参考其调用方式。

---

## 6. 下一步逆向建议

### 方案 A：捕获 CRTC 模式初始化（推荐先做）

用 DOSBox-X 调试器在 **OPEN.EXE 运行阶段**（开机 → 主菜单前）下断点：

```
bp 3D4        ; 端口 03D4 写断点（CRTC 地址寄存器）
bp 3D5        ; 端口 03D5 写断点（CRTC 数据寄存器）
bpint 10      ; INT 10h 调用断点（模式设置 AH=00）
```

记录模式设置时的寄存器序列，重点看：
- CRTC 垂直显示结束（3D4 索引 0x12，640×400 时=0x18F，640×480 时=0x1DF）
- CRTC 垂直总行（索引 0x06）
- CRTC 起始地址（索引 0x0C/0x0D）是否包含 40 行偏移
- INT 10h AH=00 的 AL 模式号（12h=640×480，11h/其他=640×400）

### 方案 B：追踪清屏/绘制行数

在 094F:24B0 清屏例程（`repe stosb` + `add di,0050`）处下内存/执行断点，回溯调用方，找出：
- 外层循环的行数计数器（当前 400？）
- 绘制起点 EDI（A000:xxxx 是否从 0x1400=40行×80字节 开始，即 y=40 对应）

若 EDI 初始 = 0x1400（40×80），说明内容从显存第 40 行开始 → 黑边由“内容下移 40 行”造成；若 EDI=0 且循环 400 次，说明只绘制 400 行。

### 方案 C：运行时 dump 显存

用调试器 dump `ES:A000` 前若干 KB，观察 y=0..39 是否为全 0（未填充），确认是“没画”还是“画了但被裁剪”。

### 方案 D：搜索常量

在 MAIN.EXE / OPEN.EXE 运行时内存中搜索：
- 0x190（400）、0x1F4（500）、0x28（40）等高度相关常量
- 模式号 0x12（18）附近代码

---

## 7. 旧对话中与此相关的用户诉求（背景）

- 用户曾要求：生成桌面快捷方式，双击启动 DOSBox 运行 `play.bat`，保证**游戏高宽比固定、不拉伸画面，视频输出 opengl perfect**（此需求已实现，与黑边无关，但说明用户在意画面比例）。
- 用户明确黑边与 DOSBox 无关（已排除渲染器/输出方式因素）。
- 若最终确认黑边是游戏自绘（方案 B 结论），去黑边可能需要修改 MAIN.EXE 绘制范围或 OPEN.EXE 模式设置；若确认是显示起始偏移（方案 A 结论），修改 CRTC 寄存器序列即可。

---

## 8. 已知约束

- OPEN.EXE / MAIN.EXE 文件本体反汇编搜不到 INT 10h / 端口写 → 内容压缩或加密，静态分析需先解压或直接运行时调试。
- 日志窗口从主菜单才开始记录，模式初始化代码在日志覆盖范围之外，需新开调试会话从启动开始记录。
- 游戏为 16 色 4 平面模式，每行 0x50 字节；改动显示范围时需保持平面/字节对齐。
